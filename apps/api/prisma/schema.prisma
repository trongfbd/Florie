generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// No domain models yet — this is Sprint 0 (project scaffold).
// The full business schema is designed in Sprint 1.
